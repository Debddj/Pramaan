services:
  # -------------------------------------------------------------
  # 1. FastAPI Legal Metrology Backend API Service
  # -------------------------------------------------------------
  - type: web
    name: pramaan-backend
    runtime: python
    region: oregon
    plan: free
    rootDir: backend
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    healthCheckPath: /api/v1/health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
      - key: DATABASE_URL
        fromDatabase:
          name: pramaan-db
          property: connectionString
      - key: STORAGE_BACKEND
        value: local
      - key: AUTO_SEED_DEMO_DATA
        value: "true"
      - key: TRIAGE_CONFIDENCE_THRESHOLD
        value: "0.85"
      - key: GEMINI_MODEL
        value: gemini-3.5-flash
      - key: GEMINI_API_KEY
        sync: false
      - key: SECRET_KEY
        generateValue: true

  # -------------------------------------------------------------
  # 2. Vite + React Statutory Enforcement Web Dashboard
  # -------------------------------------------------------------
  - type: web
    name: pramaan-dashboard
    runtime: static
    rootDir: web-dashboard
    buildCommand: "npm install && npm run build"
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://pramaan-backend-rct0.onrender.com/api/v1

databases:
  # -------------------------------------------------------------
  # 3. Managed PostgreSQL Database
  # -------------------------------------------------------------
  - name: pramaan-db
    region: oregon
    plan: free
    databaseName: pramaan
    user: pramaan
